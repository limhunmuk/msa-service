server:
  port: 8000

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: user-service
              uri: http://localhost:8080
              predicates:
                - Path=/api/users/**
            - id: board-service-jwt       # 인증이 필요한 부분 추가
              uri: http://localhost:8081
              predicates:
                - Path=/api/boards/**
                - Method=POST
              filters:
                - JwtAuthenticationFilter
            - id: board-service
              uri: http://localhost:8081
              predicates:
              - Path=/api/boards/**

#            - id: point-service # 식별자
#              uri: http://localhost:8082 # 전달주소
#              predicates:
#                - Path=/points/** # 전달 패턴


jwt:
  secret: jscode-secret-1234-1234-1234-1234